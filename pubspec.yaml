name: medicine_reminder
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.0.5

  # Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Notifications (Using the version suggested by your error)
  flutter_local_notifications: ^18.0.1

  # Timezone (Specific version to avoid conflict)
  timezone: ^0.9.4

  # Utilities
  intl: ^0.19.0
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  
  # Ensure you have your app icon generated or this line will cause errors.
  # If you don't have an icon yet, you can comment this out, 
  # but notifications require an icon at @mipmap/ic_launcher